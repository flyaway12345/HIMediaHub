import { AppDirSearchPipe } from './app-dir-search.pipe';

describe('AppDirSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new AppDirSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
